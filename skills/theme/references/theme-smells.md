# Theme Smells

- focus ring does not separate from the surface
- disabled controls look enabled
- overlays blend into the page background
- placeholder text and actual values are too similar
- selected state is not obvious
- error state is only color-shifted without stronger structure
- too many component-local overrides are needed to keep the kit usable
