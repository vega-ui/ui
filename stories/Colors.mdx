import { Meta } from '@storybook/addon-docs/blocks';
import { ColorPaletteItem } from './ColorPaletteItem';

<Meta title='Theme/Colors' />

<div style={{ fontFamily: 'sans-serif', padding: 24 }}>
    {[
        'pink', 'ruby', 'red', 'orange', 'amber', 'yellow', 'topaz',
        'green', 'nephritis', 'beryl', 'blue', 'sapphirine', 'amethyst', 'purple',
        'graphite', 'gray', 'smoke'
    ].map(name => (
        <ColorPaletteItem key={name} name={name} />
    ))}
</div>